[pytest]
# Keep this directory as pytest's rootdir so `import app` resolves to the
# reconstructor's app package, not the sibling repo-root `app` (which would
# shadow it because the root pyproject sets pythonpath=["."]).
pythonpath = .
testpaths = tests
addopts = -ra
