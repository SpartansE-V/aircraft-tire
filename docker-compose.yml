services:
  api:
    build: .
    environment:
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:3000}
      PORT: ${PORT:-8000}
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    restart: unless-stopped
